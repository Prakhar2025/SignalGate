# Agent trajectories

Final deliverable 04: representative trajectories for every agent used,
easy to follow from the agent instructions to the final result, including
tool responses, feedback, retries and human checkpoints.

Two kinds of agent worked on SignalGate, and both leave readable traces.

## 1. The runtime investigator agent

The product agent. One reasoning LLM (LOCAL_MOCK for deterministic zero-key
repro; any OpenAI-compatible endpoint in LIVE mode) plus four verification
probe tools, orchestrated by deterministic Python. Its shaping prompts live
in `src/signalgate/agent/prompts.py` (versioned, `prompts@v1.0.0`); its
contract is simple and deliberate:

- the agent plans probes and writes the narrative;
- it never computes the verdict: thresholds in
  `src/signalgate/orchestrator/thresholds.py` decide, from probe numbers.

Representative runs, regenerated by
`python -m scripts.export_trajectories` (byte-identical, seed 20260828):

| File | Outcome | What it shows |
|---|---|---|
| [`runs/s0_01.md`](runs/s0_01.md) | `PROMISING` | A sound signal passing all four probes with receipts. |
| [`runs/f2_01.md`](runs/f2_01.md) | `REJECT_SPURIOUS` | Lookahead hidden in prose: IC collapses 0.475 to 0.023 under leak-proof re-execution. |
| [`runs/f4_01.md`](runs/f4_01.md) | `REJECT_SPURIOUS` | Best-of-40 in-sample selection: raw p 0.015 survives naively, deflated p 0.45 does not. |
| [`runs/f2_06.md`](runs/f2_06.md) | `REJECT_SPURIOUS` | Half-weight contamination: the honest middle case. |
| [`runs/f3_01.md`](runs/f3_01.md) | `REJECT_SPURIOUS` | Survivor universe: the edge exists only in the as-written execution. |
| [`runs/INDEX.md`](runs/INDEX.md) | | Index with the generation stamp. |
| [`chaos.md`](chaos.md) | `NEEDS_REVIEW` (DEGRADED) | Model-down and probe-timeout paths; disclosure codes, never silent failures. |

Each trajectory contains: the untrusted input spec, the agent instructions,
the full turn-by-turn span list (instruction, tool response, feedback,
checkpoint), every tool call with its numeric response, the claims the agent
extracted, and the code-computed outcome.

Every investigation also persists the machine-readable form:
`artifacts/runs/<run_id>/bundle.json` plus `trajectory.json`.

## 2. The coding agents that built the repository

The repository was built with coding agents (ZCode CLI, model GLM-4.6 via
glm-plan, August 2026 sprint). The honest highlights of those sessions,
including the failures that changed the design:

- **Locked docs first.** The six specification documents were written and
  frozen before implementation; the agent implemented to them, not around
  them.
- **Calibration loop (the biggest real iteration).** The first synthetic
  world broke the docs' own claims: momentum priced negative, the
  permutation null was degenerate for slow signals, delisting labels
  poisoned every cross-sectional statistic, and the survivorship bite
  vanished when crashers stopped being former winners. Fixes became design:
  within-day permutation shuffles (the statistically correct null),
  near-permanent alpha characteristics, forward returns masked NaN within
  one holding period of a delisting, crashers drawn from top-alpha names,
  liquidity floors in the sound templates. Roughly six full calibration
  sweeps, each measured, before the generator was locked.
- **Bugs caught by the harness, not by hope.** The pytest suite surfaced an
  unhandled `pct_change(x, -k)` lookahead the AST scanner missed, a composer
  that could reject on a skipped probe, a token-metering gap that made
  ablation cost claims empty, and a rate-limit test that was really a
  sliding-window lesson. Each fix has a test.
- **Human checkpoints.** The owner interrupted the build several times; the
  interruption log is part of the record: Docker work stopped mid-build on
  instruction (image never pulled to completion), typography policy change
  (no em dashes anywhere, enforced by a scrubber across 56 files), frontend
  direction change (from server-rendered gate to the Next.js product
  surface), and a strategy review where the pivot question was raised and
  answered with the rubric math above.
- **Honest scope notes.** Improvement-changelog stages iter1 and iter3 are
  deterministic LOCAL_MOCK simulations of the documented behaviors, flagged
  as such in IMPROVEMENT_CHANGELOG.md; LIVE mode routes the identical
  prompt contracts to a configured model, and `scripts/routing_ritual.py`
  records honest skips without credentials.

## Reproduce

```bash
python -m scripts.export_trajectories          # regenerate trajectories/
python -m signalgate.eval.repro                # prove the numbers are byte-identical
pytest                                         # 62 tests, including the slow repro check
```
