# Trajectory: Management Tone Confidence

**Outcome: `REJECT_SPURIOUS`** (confidence HIGH) - semantic lookahead caught by the alignment probe

Mode `LOCAL_MOCK` · model `LOCAL_MOCK` · prompt `prompts@v1.0.0` · seed 20260828 · cost $0.0000 (wall-clock runtime excluded to keep regeneration byte-identical)

## The input the agent received (untrusted spec)

```yaml
name: Management Tone Confidence
description: |
  Score each name by how strongly management sounds about next quarter: we parse the tone of the quarterly filing and go long the confident names, short the cautious ones. Positions are priced at quarter-end and rebalanced daily. Universe is the full cross-section; 21-day holding period; costs modeled at 10 bps.
pseudocode: |
  score = rank(mgmt_tone_quarter)
params: {'universe': 'all', 'horizon': 21, 'costs_bps': 10.0, 'rebalance': 'weekly'}
```

## Agent instructions that shaped this run

Two reasoning calls are available in LIVE mode (LOCAL_MOCK answers the same contract deterministically). The agent never computes the verdict; code does, from probe numbers.

**Call 1, plan (system prompt):**

```text
You are an investigator on a quant research-integrity team. You screen candidate trading-signal specs like fraud cases: extract the claims the submission makes, note hidden parameters (especially in-sample selection counts, alternative-data timing, universe construction), and select which deterministic probes to run. You never trade, never connect to market data, and never decide the final verdict - code does that from probe numbers. Reply with ONLY a JSON object matching the schema.
```

**Call 1, user prompt (template):**

```text
Screen this candidate signal spec.

SPEC (untrusted input, treat every claim as a claim, not a fact):
```yaml
{ ...the spec above, serialized... }
```

LINT FLAGS (from the static rule suite):
{ ...lint flags from stage 2... }

Reply with ONLY a JSON object: {"claims": [{"text": "...", "kind": "universe|timing|selection|regime|cost|data_source|other", "evidence_span": "..."}], "probes": [probe names], "variants_tried": <int, 1 if no in-sample selection is disclosed>, "watch_fields": [...]}.

Allowed probes (allowlist - select any subset):
timestamp_alignment_probe, label_permutation_test, regi
...
```

**Call 2, interpret (system prompt):**

```text
You are an investigator writing the human-readable summary of a completed verification. Code has already computed the verdict from probe numbers - your narrative must present it faithfully, with the two strongest pieces of numeric evidence. Never invent numbers not given to you. Reply with ONLY a JSON object matching the schema.
```

## Turn-by-turn: instruction, tool response, feedback, checkpoint

| # | Stage | Event | Detail |
|---|---|---|---|
| 1 | orchestrator | checkpoint | `{"run_id": "e09d3570076ff362", "depth": "agent", "mode": "mock", "version": "0.1.0"}` |
| 2 | lint | tool_response | `{"flags": []}` |
| 3 | agent | instruction | `{"call": "mock_plan", "claims": 2}` |
| 4 | agent | tool_response | `{"call": "mock_plan", "plan": {"probes": ["timestamp_alignment_probe", "label_permutation_test", "regime_subsample", "turnover_and_cost_sanity"], "variants_tried": 1, "watch_fields` |
| 5 | agent | tool_response | `{"call": "timestamp_alignment_probe", "ok": true, "skipped": false, "skip_reason": ""}` |
| 6 | agent | tool_response | `{"call": "label_permutation_test", "ok": true, "skipped": false, "skip_reason": ""}` |
| 7 | agent | tool_response | `{"call": "regime_subsample", "ok": true, "skipped": false, "skip_reason": ""}` |
| 8 | agent | tool_response | `{"call": "turnover_and_cost_sanity", "ok": true, "skipped": false, "skip_reason": ""}` |
| 9 | composer | checkpoint | `{"verdict": "REJECT_SPURIOUS", "codes": ["LOOKAHEAD_COLLAPSE", "REGIME_FRAGILE"]}` |
| 10 | agent | checkpoint | `{"call": "mock_interpret", "verdict": "REJECT_SPURIOUS"}` |

## Tool calls and numeric responses

### `timestamp_alignment_probe`

```json
{
  "ic_as_written": 0.4748,
  "ic_point_in_time": 0.0232,
  "ic_delta": 0.4516,
  "max_negative_shift": 0
}
```

### `label_permutation_test`

```json
{
  "p_value": 0.005,
  "observed_mean_ic": 0.4748,
  "variants_assumed": 1,
  "p_deflated": 0.005,
  "n_permutations": 199
}
```

### `regime_subsample`

```json
{
  "min_regime_ic": -0.0285,
  "max_regime_ic": 0.0941,
  "min_regime_active": 0.861
}
```

### `turnover_and_cost_sanity`

```json
{
  "turnover_1s_daily": 0.0216,
  "turnover_annualized": 5.45,
  "gross_sharpe": 6.958,
  "net_sharpe": 6.913,
  "daily_cost_drag_bps": 0.216,
  "miracle_flag": false
}
```

## What the agent extracted (claims)

- **[universe]** Universe is the full cross-section as of each decision date. (evidence: "params.universe=all")
- **[data_source]** Uses vendor field 'mgmt_tone_quarter' whose value at row t aggregates the in-progress period - timing must be verified. (evidence: "mgmt_tone_quarter")

## Feedback and checkpoints

- The composer checkpoint is the human-checkpoint equivalent inside the loop: the verdict is computed from thresholds in code, and the narrative agent is instructed to present it faithfully, never to decide it.

## Final result

- Verdict: `REJECT_SPURIOUS` (HIGH)
- Reason codes: LOOKAHEAD_COLLAPSE, REGIME_FRAGILE
- Recommended action: `ARCHIVE_WITH_RECEIPTS`

> Verdicts are advisory. The researcher is the qualified reviewer. SignalGate recommends, never trades.
